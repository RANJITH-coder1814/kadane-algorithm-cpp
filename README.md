# Kadane's Algorithm in C++

This repository contains the C++ solution for finding the **Maximum Subarray Sum** using **Kadane’s Algorithm**.

## Problem Statement

Given an integer array `nums`, find the contiguous subarray with the largest sum and return that sum.

---

## Example

### Input
```cpp
nums = [2, 3, 5, -2, 7, -4]
